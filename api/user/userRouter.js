const router = require('express').Router();
/*
  GET /api/user/tickets 

  Student: all of the token owner's created tickets

  Helper: all of the token owner's accepted tickets
  
*/

router.get('/', (req, res) => {

});

module.exports = router;